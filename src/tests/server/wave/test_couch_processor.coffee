testCase = require('nodeunit').testCase
sinon = require('sinon-plus')

module.exports =
    CouchWaveProcessorTest: testCase
        setUp: (callback) ->
            callback()

        tearDown: (callback) ->
            callback()
